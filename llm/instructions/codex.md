# Codex Instructions

Follow `llm/instructions/base.md` as the source policy.

Use Codex subagents only when the user explicitly asks for parallel or specialized agent work. Use shared skills for repeatable workflows; users can invoke them with skill mentions such as `$review` or `$commit`.
